import React from "react";

const InputContainer = props => {
  return (
    <div className="input-container">
      <textarea rows="4" cols="125" />
      <button>Send</button>
    </div>
  );
};

export default InputContainer;
