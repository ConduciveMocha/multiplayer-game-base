import * as UiActions from '../constants/action-types/ui-types'
import FocusStates from '../constants/FocusStates'

const uiInitialState = {currentFocus:FocusStates.GAME};

export default function uiReducer(state=uiInitialState, action) {
    switch(action.type) {
        
    }
}